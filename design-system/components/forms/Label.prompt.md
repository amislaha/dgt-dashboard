Use `Label` above every form control; `htmlFor` is required.

```jsx
<Label htmlFor="kawasan" required>Kawasan Transmigrasi</Label>
```

Sentence case, no colon. Mark optional fields in the hint instead of marking most fields required.
