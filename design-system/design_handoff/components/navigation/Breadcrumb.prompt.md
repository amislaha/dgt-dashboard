Use `Breadcrumb` on every detail page — government hierarchies are deep and users arrive from links.

```jsx
<Breadcrumb items={[{ label: "Beranda", href: "#" }, { label: "Kawasan", href: "#" }, { label: "KTM Lamunti" }]} />
```
