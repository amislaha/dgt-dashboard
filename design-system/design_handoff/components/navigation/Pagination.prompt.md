Use `Pagination` under every `Table`.

```jsx
<Pagination page={page} totalPages={14} totalItems={1284} onChange={setPage} />
```

Counts use Indonesian formatting (1.284) — pass raw numbers, the component formats.
