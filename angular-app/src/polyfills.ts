/**
 * Angular 8 polyfills. This app targets es2015 output (see tsconfig.json), so most
 * of the legacy ES5/IE polyfill list from a stock `ng new` output is intentionally
 * omitted — uncomment as needed if you must support older browsers.
 */
import 'zone.js/dist/zone';
