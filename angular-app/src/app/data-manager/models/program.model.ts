/** One of the 5 transmigration program types (Tuntas, Lokal, Patriot, Karya Nusa, Gotong Royong). */
export interface Program {
  id: string;
  jenisTransmigrasi: string;
  singkatan: string;
  keterangan: string;
}
